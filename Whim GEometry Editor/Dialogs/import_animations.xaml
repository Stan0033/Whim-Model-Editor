<Window x:Class="Whim_GEometry_Editor.Dialogs.import_animations"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Whim_GEometry_Editor.Dialogs"
        mc:Ignorable="d"
        KeyDown="Window_KeyDown"
        Background="LightGray"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="Import animations" Height="150" Width="350">
    <StackPanel Margin="10">
        <CheckBox IsEnabled="False" IsChecked="True"  >Overwrite keyframes for identically-named nodes</CheckBox>
        
        <CheckBox  Name="check_addsequences"   >Also add the missing animated sequences, if possible</CheckBox>
        <Button Padding="10" Margin="10" Click="import">Import</Button>
    </StackPanel>
</Window>
